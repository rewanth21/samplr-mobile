box: node:4.2.3

build:
  steps:
    - script:
        name: build info
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"

  after-steps:
    - slack-notifier:
        url: https://msd2016.slack.com/
        channel: https://msd2016.slack.com/messages/led/
